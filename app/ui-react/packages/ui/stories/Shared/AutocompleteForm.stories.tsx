import { storiesOf } from '@storybook/react';
import * as React from 'react';

const stories = storiesOf('Shared/AutocompleteForm', module);

stories.add('autocomplete form', () => (
  <p>Testing.</p>
));
