describe('Create a Connection', () => {
  beforeEach(function() {
    cy.visit('/');
  });

  /**
   * Happy Path
   * 1.
   */

  it('loads the Create Connection page successfully', () => {
    cy.visit('/');
  });

  /**
   * Alternative Path
   */

  it('', () => {});

  /**
   * Exception Path
   */

  it('', () => {});
});
