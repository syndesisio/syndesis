import { Endpoints } from '@syndesis/ui/platform';

export const apiProviderEndpoints: Endpoints = {
  validateOpenApiSpecification: '/apis/info',
  getIntegrationFromSpecification: '/apis/generator',
};
