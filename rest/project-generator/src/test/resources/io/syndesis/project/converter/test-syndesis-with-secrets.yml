---
flows:
- steps:
  - kind: endpoint
    uri: periodic-timer-connector
    properties:
      period: "5000"
  - kind: endpoint
    uri: http-get-connector
    properties:
      password: '{{http-get-connector.password}}'
      httpUri: http://localhost:8080/hello
      username: '{{http-get-connector.username}}'
